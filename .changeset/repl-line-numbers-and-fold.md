---
"@jschofield/repl-playground": minor
---

Add `line-numbers` attribute to opt into CodeMirror line numbers, and `fold` attribute for code folding (e.g. `fold="1,5,23-34"` — single line numbers use language-aware folding, ranges fold the specified lines explicitly).
