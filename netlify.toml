[build]
  publish = "_site"
  command = "pnpm build"

[build.environment]
  NODE_VERSION = "22"

[functions]
  directory = "netlify/functions"

[dev]
  command = "pnpm start"
  publish = "_site"
  framework = "#auto"
  targetPort = 8080
